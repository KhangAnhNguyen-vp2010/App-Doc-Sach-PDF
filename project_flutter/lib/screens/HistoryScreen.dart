import 'package:flutter/material.dart';

import 'Navigation.dart';

class HistoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Màn hình Lịch sử")),

    );
  }
}